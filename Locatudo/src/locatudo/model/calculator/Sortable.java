package locatudo.model.calculator;

//Como não é possível criar mais de um extends,
// então cria-se uma interface para poder atribuir notas aos filmes e séries.

public interface Sortable {
    double getClassification(); //Não precisa ser atribuído valor a interface

}
